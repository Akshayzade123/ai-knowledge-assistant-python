"""
Services - Business logic layer.

SOLID Principles Applied:
- Single Responsibility (S): Each service handles one business domain
- Dependency Inversion (D): Services depend on interfaces, not implementations
- Open/Closed (O): Services are open for extension, closed for modification
"""
